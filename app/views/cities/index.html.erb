<center>
	<h1>Cidades</h1>
	
	<%= form_tag(find_path, :method => :get) do %>
		<%= text_field_tag 'find', nil, placeholder: 'Digite o nome da cidade...'%>
		<%= submit_tag "Pesquisar" %>
	<% end %>

	<table>
			<%cont = 0%>
			<%@cities.each do |f| %>
					<%if cont<5 %>
						<td><%= link_to f.name, city_path(f) %></td>
						<td>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</td>
					<%else%>
						<tr>
							<td><%= f.name %></td>
						</tr>
					<%end%>
				<%cont=cont+1%>
			<%end%>	
	</table>
</center>